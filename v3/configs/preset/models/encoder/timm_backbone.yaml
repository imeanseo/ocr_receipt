# @package _global_

models:
  encoder:
    _target_: ${encoder_path}.TimmBackbone
    model_name: 'resnet50'           # v3: ResNet34 → ResNet50 (더 강력한 feature extraction)
    select_features: [1, 2, 3, 4]            # Output layer
    pretrained: true
